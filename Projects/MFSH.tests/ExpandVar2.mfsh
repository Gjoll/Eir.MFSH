#macro Alpha(AlphaName)
  `{
  `  "graphNode" : {
  `  }
  `},
#end

#macro Beta(BetaName)
#apply Alpha("BetaName")
#end

Profile: Alpha
#apply Beta("%Profile%")
#apply Beta("One")
#apply Beta("Two")
