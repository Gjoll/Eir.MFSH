#macro Alpha(AlphaName)
  `{
  `  "graphNode" : {
  `  "graphName" : "AlphaName",
  `  }
  `},
#end

#macro Beta(BetaName)
#apply Alpha("BetaName")
#end

Profile: Alpha
#apply Beta("%Profile%")
#apply Beta("One")
#apply Beta("Two")
